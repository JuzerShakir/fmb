<% if @sabeels.any? %>
  <%= turbo_stream.append :sabeels, partial: "sabeel" %>
<% else %>
  <%= turbo_stream.append :sabeels, partial: "shared/no_results" %>
<% end %>

<%= render partial: "shared/pagy",
  locals: {path: sabeels_active_path(format: :turbo_stream, page: @pagy.next)}
%>