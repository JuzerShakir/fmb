<%# * Title %>
<h2 class="heading">Sabeels</h2>

<%# * search %>
<%= render partial: "shared/search",
      locals: {attr_name: "ITS", path: sabeels_path, search: :slug_start} %>

<%# * show all sabeels %>
<%= render partial: "shared/results",
      locals: {model: "sabeels", path: sabeels_path(format: :turbo_stream, q: params[:q]&.to_unsafe_h)} %>
