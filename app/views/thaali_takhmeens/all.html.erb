<%# * Title %>
<h2 class= "text-center">Takhmeens in <span class="text-secondary"><%= "#{@year}" %></span> </h2>

<%# * Total No. of Takhmeens %>
<p class="fs-5 mt-5 fw-bold text-info text-center">Total number of Takhmeens: <%= @total %></p>

<%# * show all takhmeens %>
<%= render partial: "shared/results",
    locals: { instances: @thaalis, id: "thaalis", path: thaali_takhmeens_all_path(format: :turbo_stream) } %>