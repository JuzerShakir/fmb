<%# * Title %>
<h2 class="text-center">Show User</h2>

<%# * show %>
<%= render "show" %>

<%# * action buttons %>
<%= render partial: "shared/actions",
    locals: { edit_path: edit_user_path(@user) } %>

<%# * destroy Modal %>
<%= render partial: "shared/destroy_modal",
     locals: {destroy_path: @user, message: "delete User: #{@user.name}?" } %>