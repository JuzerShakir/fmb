<%# * Title %>
<h2 class= "text-center">Sabeels</h2>

<%# * No. of Sabeels %>
<p class="mt-3 fw-bold text-center text-info">Total number of Sabeels: <%= Sabeel.count %></p>

<%# * search %>
<%= render partial: "shared/search", locals: { message: "Name or ITS No." ,
        path: all_sabeels_path, search: :name_or_its_cont } %>

<%# * show all sabeels %>
<%= render partial: "shared/results",
    locals: { instances: @sabeels, id: "sabeels", path: all_sabeels_path(format: :turbo_stream, q: params[:q]&.to_unsafe_h) } %>