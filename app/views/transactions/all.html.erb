<%# * Title %>
<h2 class="text-center">Transactions</h2>

<%# * search %>
<%= render partial: "shared/search",
      locals: {message: "Recipe Number", path: transactions_all_path, search: :recipe_no_cont} %>

<%# * show all transactions %>
<%= render partial: "shared/results",
      locals: {id: "transactions", path: transactions_all_path(format: :turbo_stream, q: params[:q]&.to_unsafe_h)} %>
