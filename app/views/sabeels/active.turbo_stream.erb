<% if @sabeels.any? %>
  <%= turbo_stream.append :sabeels, partial: "sabeels" %>
<% else %>
  <%= turbo_stream.append :sabeels, partial: "shared/no_results" %>
<% end %>

<%= render "shared/pagy", path: sabeels_active_path(format: :turbo_stream, page: @pagy.next) %>